import 'package:get/get.dart';

class AppIndicatorsVM extends GetxController {
  var cartItemCount = 0.obs;
  var favoriteItemCount = 0.obs;
  var notificationCount = 0.obs;
  var orderItemCount = 0.obs;
  var amountPaid=false.obs;

// You might also have methods to increment/decrement these,
// or they could be updated directly by other VMs.
}